"use client";
import { Building2, Palette } from "lucide-react";
import { PageHeader } from "@/components/layout/page-header";
import { Card } from "@/components/ui/card";
import { useAuth } from "@/hooks/use-auth";
export default function SettingsPage() { const { user } = useAuth(); return <><PageHeader eyebrow="Configuration" title="School settings" description="Your school profile and appearance preferences." /><div className="grid gap-6 lg:grid-cols-2"><Card className="p-6"><Building2 className="text-[var(--primary)]" /><h2 className="mt-4 font-display text-xl font-bold">School information</h2><dl className="mt-5 space-y-3 text-sm"><div className="flex justify-between gap-4"><dt className="text-[var(--muted-foreground)]">School</dt><dd className="font-semibold">{user?.schoolName ?? "Not configured"}</dd></div><div className="flex justify-between gap-4"><dt className="text-[var(--muted-foreground)]">Administrator</dt><dd className="font-semibold">{user?.firstName} {user?.lastName}</dd></div><div className="flex justify-between gap-4"><dt className="text-[var(--muted-foreground)]">Email</dt><dd className="font-semibold">{user?.email}</dd></div></dl></Card><Card className="p-6"><Palette className="text-[var(--accent)]" /><h2 className="mt-4 font-display text-xl font-bold">Appearance</h2><p className="mt-2 text-sm text-[var(--muted-foreground)]">Use the theme switch in the top bar to choose light or dark mode. Your preference is saved on this device.</p></Card></div></>; }
