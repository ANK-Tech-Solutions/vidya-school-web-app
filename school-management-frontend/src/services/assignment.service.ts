import { api } from "@/services/api"; import type { ApiResponse } from "@/types/api"; import type { PageResponse } from "@/types/common"; import type { Assignment, DriverAssignmentPayload, StudentAssignmentPayload } from "@/types/assignment";
const base = "/api/v1/admin/assignments";
export const assignmentService = { list: async () => (await api.get<ApiResponse<PageResponse<Assignment>>>(base)).data.data, assignDriver: async (payload: DriverAssignmentPayload) => (await api.post<ApiResponse<Assignment>>(`${base}/drivers`, payload)).data.data, assignStudent: async (payload: StudentAssignmentPayload) => (await api.post<ApiResponse<Assignment>>(`${base}/students`, payload)).data.data, deactivate: async (id: number) => (await api.patch<ApiResponse<Assignment>>(`${base}/${id}/deactivate`)).data.data };
